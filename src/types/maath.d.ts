declare module 'maath/random' {
  export function inSphere(buffer: Float32Array, options?: { radius?: number; position?: [number, number, number] }): Float32Array;
  // Add other functions if needed
}
